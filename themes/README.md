Unzip a theme into ~/.themes, then use gnome tweaks to select the theme under
"appearance". You may also need to install the gnome "user themes" extension,
then the theme may also show up as a "shell" theme.
